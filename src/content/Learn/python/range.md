---
title: "range"
date: 2024-10-06T19:08:00Z
tags: ["python"]
---
# range 用法

`range` 生成一个整数序列，左闭右开（不包含 `stop`）。

## 形式

- `range(stop)`
- `range(start, stop)`
- `range(start, stop, step)`

## 示例

```python
list(range(5))
list(range(1, 5))
list(range(1, 10, 2))
```
